# TEST Directory

Place your chloroplast sequence files here (`.fasta`, `.fa`, `.fna`, `.gb`, or `.gbk`) to run the quick test:

```bash
perl ECuADOR.pl --input_dir TEST --window 800 --out ALL --orient TRUE
```
